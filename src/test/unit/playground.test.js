describe("basic math", () => {
  it("adds two numbers", () => {
    // what i expect
    expect(1 + 1).toBe(2);
  });
  // one test
  it("subtract two numbers", () => {
    //  and one expect
    expect(5 - 3).toBe(2);
  });
});
